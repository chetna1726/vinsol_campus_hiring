<% if object.errors.any? %>
  <div id="error_explanation", class='alert alert-danger'>
    <h5><%= pluralize(object.errors.count, "error") %></h5>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>